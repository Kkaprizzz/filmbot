name: Deploy KinoBot

on:
  push:
    branches:
      - main
  workflow_dispatch: # Позволяет запускать вручную из вкладки Actions

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Deploy to Server via SSH
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          password: ${{ secrets.SERVER_PASSWORD }}
          script: |
            # Определяем рабочую директорию (автоматически подставит /root или /home/user)
            WORKDIR=$HOME/filmbot
            mkdir -p $WORKDIR
            cd $WORKDIR
            
            git config --global --add safe.directory $WORKDIR
            
            if [ -d ".git" ]; then
              echo "Repository exists, pulling latest changes..."
              git pull origin main
            else
              echo "Cloning repository..."
              git clone https://github.com/Kkaprizzz/filmbot.git .
            fi
            
            # Пробуем запустить через новый 'docker compose' или старый 'docker-compose'
            if docker compose version > /dev/null 2>&1; then
              docker compose down
              docker compose up -d --build
            else
              docker-compose down
              docker-compose up -d --build
            fi
